<%= title l('toggl.toggl_entry') %>

<%= labelled_form_for @toggl_entry, :url => toggl_entries_path do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= submit_tag l(:button_create) %>
  <%= submit_tag l(:button_create_and_continue), :name => 'continue' %>
<% end %>

